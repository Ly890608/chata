name: Windows

on:
  push:
    branches:
    - master

jobs:
  build-windows:
    name: build on windows
    runs-on: windows-latest
    
    steps:
    - uses: actions/checkout@v1
    - name: build
      run: |
        echo "Hello"
      shell: cmd
